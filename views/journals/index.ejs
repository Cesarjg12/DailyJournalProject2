<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Morning</th>
      <th>Noon</th>
      <th>Evening</th>
    </tr>
  </thead>
  <tbody>
    <% journals.forEach(function(m) { %>
      <tr>
        <td><%= m.title %></td>
        <td><%= m.releaseYear %></td>
        <td><a href="/journals/<%= m._id %>">DETAILS</a></td>
      </tr>
    <% }); %>
  </tbody>
</table>

<%- include('../partials/footer') %>